/**
 * A text style configuration object as used by `BitmapText#addTextStyle`.
 *
 * @typedef {object} Phaser.Types.GameObjects.BitmapText.TextStyleConfig
 * @since 4.3.0
 *
 * @property {string} [font] - The key of the Bitmap Font in the cache. Omit to inherit the BitmapText's font.
 * @property {number} [size] - Font size for runs using this style. Omit to inherit the BitmapText's `fontSize`.
 * @property {number} [color=0xffffff] - Tint color for runs using this style (0xRRGGBB).
 */
